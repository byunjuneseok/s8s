// Package config loads and validates the s8s configuration file
// (~/.s8s/config.yaml), which follows a kubeconfig-style layout of contexts,
// brokers, and credentials, with a current-context selector.
package config
